[project]
name = "psv-elevation-control"
version = "0.1.0"
description = "Control de relés HW-383 con NodeMCU (ESP8266) + MicroPython"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn",
    "pyserial",
    "esptool==4.7.0",
]
